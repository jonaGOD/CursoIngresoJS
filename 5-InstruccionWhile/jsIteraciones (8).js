function mostrar()
{
	var contador=0;
	var positivo=0;
	var negativo=1;
	var respuesta='si';
	var numero;
	var acumulador;

	while (respesta == "si") {
		numero = prompt("Ingrese el número");
		if (numero < 0) {
			acumulador = parseInt(numero) + parseInt()
		}

	}


document.getElementById('suma').value=positivo;
document.getElementById('producto').value=negativo;

}//FIN DE LA FUNCIÓN